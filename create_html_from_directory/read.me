You should properly install libmediainfo-dev,

If you are using Ubuntu or debian run:

sudo apt-get update -y
sudo apt-get install -y libmediainfo-dev

If you're using redhat, centos or fedora run:

yum install libmediainfo

